from distutils.core import setup

setup(
    name         = 'approot',
    version      = '0.1.0',
    description  = 'On import finds the application root directory',
    author       = 'Pete Chudykowski',
    author_email = 'p@chudpi.org',
    py_modules   = ['approot'],
)

